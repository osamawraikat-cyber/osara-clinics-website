name: Temporary PR2 physician review approval
on:
  push:
    branches:
      - seo/pr2-service-intent
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: seo/pr2-service-intent
          fetch-depth: 0
      - name: Apply physician-approved wording and documentation
        run: python .github/pr2_review_approval.py
      - name: Validate diff
        run: git diff --check
      - name: Commit review decisions
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add psoriasis-treatment.html acne-scar-treatment.html MEDICAL_REVIEW_REQUIRED.md
          git commit -m "Record PR2 physician review approvals"
          git push origin HEAD:seo/pr2-service-intent
